{
  "name": "wikipedia-trends",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent Skill для аналізу переглядів Wikipedia",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "wikipedia-trends": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --dir tests"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "@types/pdfkit": "^0.17.6",
    "@types/svg-to-pdfkit": "^0.1.4",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "pdfkit": "^0.20.2",
    "svg-to-pdfkit": "^0.1.8"
  }
}
